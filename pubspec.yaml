name: sliver_overfill_remaining
description: "A sliver that always fills the remaining space in the viewport, plus some extra height."
version: 0.1.0
repository: https://github.com/martinsellergren/sliver_overfill_remaining

environment:
  sdk: ^3.5.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  sliver_tools: ^0.2.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
